#include<stdio.h>

int main(){
    int principal=33,rate=22,year=2;
    int simpleinterest = (principal*rate*year)/100;
    printf("the value of simpleinterest is %d", simpleinterest);
return 0;
}